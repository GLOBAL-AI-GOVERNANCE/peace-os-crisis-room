# Windows Build Folder

Place exported Windows files here after Godot runtime testing.

Expected public build candidate output:

```text
PeaceGovernanceCrisisRoom_v0.2.2.exe
PeaceGovernanceCrisisRoom_v0.2.2.pck
```

Or package them as:

```text
PeaceGovernanceCrisisRoom_v0.2.2_Windows.zip
```

This repository package currently contains source only, not a verified executable.
