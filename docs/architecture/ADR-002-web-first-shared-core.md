# ADR-002: Web-first shared-core architecture

The semantic web client is the public review front door. The Godot client remains the advanced facilitated and offline path. Both consume byte-identical generated copies of the authoritative `core/` data and must agree with the Python scoring oracle through golden-vector tests.
